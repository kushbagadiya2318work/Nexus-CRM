# Force npm to install devDependencies on Vercel even when NODE_ENV=production.
# Without this, `tsc`, `vite`, and `@vitejs/plugin-react` may be skipped during
# `npm install`, causing the build step to fail silently or produce no output.
production=false
