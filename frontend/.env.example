# Copy to `.env.local` for local dev or `.env.production` for prod builds.
# Local dev (standalone backend):
# VITE_CRM_API_URL=http://localhost:4000/api

# Production (Vercel-hosted backend):
VITE_CRM_API_URL=https://nexus-crm-v3x7.vercel.app/api
